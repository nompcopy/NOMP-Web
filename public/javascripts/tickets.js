// Global tickets.js

// ==== DOM Ready =====
$(document).ready(function() {
  // Populate the user table on initial page load
  populateOfferList();
});

function populateOfferList() {
    var tableContent = '';
}